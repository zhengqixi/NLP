from nltk import word_tokenize
import Paragraph

class Naive_Bayes:

    def __init__(self, n):
       self.token_probability= list()
       self.tokens = list()
       for i in range(0, n):
           tokens.append(list())

